# Codex branch rules

- Áp dụng cho nhánh `codex`.
- Mọi loại sửa code, text, tài liệu đều đi qua nhánh này nếu task thuộc Codex.
- Không sửa chéo sang nhánh `agy`.
- Khi hoàn tất một phần việc, commit hoặc chuẩn bị push đúng nhánh `codex` trước khi chuyển sang nhánh khác.
- Nếu tạo nhánh tổng hợp `dev`, chỉ merge các thay đổi đã được xác nhận từ `agy` và `codex`.
- Sau khi test ổn trên `dev`, mới merge về `main` và đẩy lên GitHub.

